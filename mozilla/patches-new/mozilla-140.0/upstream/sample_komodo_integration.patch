diff --git a/browser/app/nsBrowserApp.cpp b/browser/app/nsBrowserApp.cpp
index 1234567..abcdefg 100644
--- a/browser/app/nsBrowserApp.cpp
+++ b/browser/app/nsBrowserApp.cpp
@@ -45,6 +45,10 @@
 #include "nsXULAppAPI.h"
 #include "mozilla/UniquePtr.h"
 
+#ifdef MOZ_KOMODO
+#include "komodo/KomodoIntegration.h"
+#endif
+
 using namespace mozilla;
 
 int main(int argc, char* argv[], char* envp[])
@@ -78,6 +82,12 @@ int main(int argc, char* argv[], char* envp[])
     
     rv = XRE_main(argc, argv, envp);
     
+#ifdef MOZ_KOMODO
+    // Firefox 140 ESR: Komodo integration
+    if (NS_SUCCEEDED(rv)) {
+      KomodoIntegration::Shutdown();
+    }
+#endif
     
     return NS_SUCCEEDED(rv) ? 0 : 1;
 }

diff --git a/browser/confvars.sh b/browser/confvars.sh
index 789abc1..234def5 100644
--- a/browser/confvars.sh
+++ b/browser/confvars.sh
@@ -35,6 +35,10 @@ MOZ_APP_VERSION=$FIREFOX_VERSION
 MOZ_APP_ID={ec8030f7-c20a-464f-9b0e-13a3a9e97384}
 MOZ_BRANDING_DIRECTORY=browser/branding/official
 MOZ_OFFICIAL_BRANDING_DIRECTORY=browser/branding/official
 
+# Komodo integration
+MOZ_KOMODO=1
+ACDEFINE(MOZ_KOMODO)
+
 # Include the Firefox branding configuration.
 . "${srcdir}/branding/official/configure.sh"
 
# Enable the update packager
MOZ_PACKAGE_JSSHELL=1